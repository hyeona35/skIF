# Eval suites

An eval suite contains tasks used to compare a baseline Skill with candidate Skills. The same suite is run against every candidate. Mandatory cases are release gates; regressions are tracked per case.
