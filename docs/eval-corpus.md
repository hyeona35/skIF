# Evaluation Corpus Product

Corpus lifecycle:

`draft → proposed → reviewed → trusted → deprecated → retired`

Generated cases are never automatically trusted. Leakage checks search for answer leakage, rubric manipulation, prompt-injection style text, and near-duplicate cases. Trusted corpora expose a digest so a run can prove which benchmark it used. Benchmark results feed the leaderboard separately from the corpus itself.
