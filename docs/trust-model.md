# Trust Model

Identity and reputation are separate.

`unknown → observed → contributor → trusted → verified → maintainer → host`

Identity answers “who is this actor?”. Reputation answers “how reliable has this actor been?”. A new identity is deliberately influence-limited. Security incidents, repeated duplicate contributions, and false-positive reports reduce influence.

Federated artifacts enter quarantine before trusted registry release.
