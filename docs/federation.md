# Federation

Federation artifacts are signed, size-limited, origin-checked, and quarantined. Nodes have identity, capabilities, heartbeat, and trust state. Strict installations require a real signing key; deterministic hashes are not treated as signatures.

Release from quarantine is a host-side governance action.
