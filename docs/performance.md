# Performance Benchmarks

Run:

```bash
python scripts/perf_benchmark.py --iterations 100
```

Track event read latency, reputation calculation, graph neighborhood traversal, and cache operations. Performance budgets are configuration rather than hard-coded assumptions so deployments can tune them.
